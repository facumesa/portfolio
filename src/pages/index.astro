---
// DATOS DE TUS PROYECTOS Y EXPERIENCIAS
const proyectosPrincipales = [
  {
    titulo: "StellarMinds",
    subtitulo: "Plataforma de Gestión Astronómica & AI-Assisted Assessment",
    descripcion: "Sistema web con arquitectura distribuida en capas, autenticación segura JWT, persistencia relacional con SQL Server / EF Core, contenedorizado en Docker y desplegado en la nube. Integra la API de Google Gemini para la evaluación automatizada de equipamiento astronómico.",
    tags: [
      { nombre: ".NET 10", color: "bg-purple-950/60 text-purple-300 border-purple-800/60" },
      { nombre: "C#", color: "bg-emerald-950/60 text-emerald-300 border-emerald-800/60" },
      { nombre: "Web API REST", color: "bg-blue-950/60 text-blue-300 border-blue-800/60" },
      { nombre: "EF Core / SQL", color: "bg-cyan-950/60 text-cyan-300 border-cyan-800/60" },
      { nombre: "Docker", color: "bg-sky-950/60 text-sky-300 border-sky-800/60" },
      { nombre: "Gemini API", color: "bg-amber-950/60 text-amber-300 border-amber-800/60" },
    ],
    linkGithub: "https://github.com/facumesa/api-rest-cliente-csharp",
    linkDemo: "https://stellarminds-web.onrender.com",
    destacado: true,
  },
  {
    titulo: "Sistema de Gestión de Vuelos",
    subtitulo: "Algoritmos & Tipos de Datos Abstractos de Bajo Nivel",
    descripcion: "Construcción completa desde cero de estructuras de datos dinámicas (Listas Enlazadas, Pilas y Colas) sin utilizar librerías nativas de Java. Implementa inserciones ordenadas, gestión de ciclos de vida de vuelo y validación de documentos mediante Expresiones Regulares.",
    tags: [
      { nombre: "Java", color: "bg-orange-950/60 text-orange-300 border-orange-800/60" },
      { nombre: "TADs a Mano", color: "bg-teal-950/60 text-teal-300 border-teal-800/60" },
      { nombre: "Algoritmos O(n)", color: "bg-indigo-950/60 text-indigo-300 border-indigo-800/60" },
      { nombre: "RegEx", color: "bg-rose-950/60 text-rose-300 border-rose-800/60" },
    ],
    linkGithub: "https://github.com/facumesa/javatads",
    destacado: false,
  }
];

const proyectosIA = [
  {
    titulo: "DevDock / Personal Developer Toolkit",
    subtitulo: "AI-Assisted Development & Rapid Prototyping",
    descripcion: "Aplicación centralizada de recursos y herramientas para desarrolladores. Diseñada y estructurada aplicando flujos de trabajo con modelos de lenguaje (OpenAI Codex), enfocado en la velocidad de prototipado, arquitectura de datos y dirección de producto.",
    tags: [
      { nombre: "Codex", color: "bg-teal-950/60 text-teal-300 border-teal-800/60" },
      { nombre: "Prompt Engineering", color: "bg-pink-950/60 text-pink-300 border-pink-800/60" },
      { nombre: "AI-Native Workflow", color: "bg-violet-950/60 text-violet-300 border-violet-800/60" },
    ],
    linkGithub: "https://github.com/tu-usuario",
  }
];

const habilidades = [
  { categoria: "Backend & Lenguajes", items: [".NET 10", "C#", "Java", "PHP / Laravel", "JavaScript"] },
  { categoria: "Bases de Datos & Infra", items: ["SQL Server", "Entity Framework", "Docker", "REST APIs", "Git / GitHub"] },
  { categoria: "Frontend & Tooling", items: ["Tailwind CSS", "Astro", "Vercel", "Render", "Postman / Swagger"] }
];
---

<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Facundo Mesa | Software Developer</title>
    <style>
      @import "tailwindcss";

      /* EFECTO DE DESVANECIMIENTO AL SCROLLEAR */
      @supports (animation-timeline: view()) {
        .sticky-hero {
          animation: fadeAndScale linear both;
          animation-timeline: view();
          animation-range: exit 0% exit 100%;
        }

        @keyframes fadeAndScale {
          to {
            opacity: 0;
            transform: scale(0.92);
            filter: blur(10px);
          }
        }
      }
    </style>
  </head>
  <body class="bg-neutral-950 text-neutral-100 min-h-screen font-sans selection:bg-neutral-800 selection:text-white relative">
    
    <!-- GLOW DE FONDO -->
    <div class="fixed top-0 left-1/2 -translate-x-1/2 w-[900px] h-[500px] bg-gradient-to-tr from-indigo-600/15 via-sky-500/10 to-transparent blur-3xl pointer-events-none rounded-full -z-10"></div>

    <div class="max-w-5xl mx-auto px-6 sm:px-10">
      
      <!-- HERO SE QUEDA FIJO EN EL FONDO (STICKY) Y SE DESVANECE -->
      <header class="sticky top-0 h-screen flex flex-col justify-center space-y-8 sticky-hero z-0">
        <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-6 pb-6 border-b border-neutral-800/80">
          <div class="space-y-3">
            <div class="inline-flex items-center gap-2 px-3.5 py-1.5 rounded-full bg-neutral-900/90 border border-neutral-800 text-xs font-mono text-neutral-300 backdrop-blur-md">
              <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
              Disponible para proyectos & roles backend / full-stack
            </div>
            <h1 class="text-6xl sm:text-8xl font-black tracking-tight text-white">
              Facundo Mesa
            </h1>
            <p class="text-2xl sm:text-3xl font-light text-neutral-400">
              Backend Developer & AI-Assisted Engineer
            </p>
          </div>

          <div class="w-28 h-28 sm:w-36 sm:h-36 rounded-3xl bg-gradient-to-br from-neutral-800 to-neutral-900 border border-neutral-700/80 flex items-center justify-center text-4xl sm:text-5xl font-black text-white shadow-2xl shrink-0">
            FM
          </div>
        </div>

        <p class="text-xl sm:text-2xl text-neutral-300 font-light leading-relaxed max-w-3xl">
          Especializado en la creación de <strong class="text-white font-semibold">APIs REST distribuidas</strong>, arquitectura limpia en <strong class="text-white font-semibold">.NET y Java</strong>, e integración de IA.
        </p>

        <div class="flex flex-wrap items-center gap-4 pt-4">
          <a href="#contenido" class="px-6 py-3.5 rounded-xl bg-white text-neutral-950 font-bold hover:bg-neutral-200 transition-all duration-200 shadow-xl shadow-white/5">
            Ver Proyectos ↓
          </a>
          <a href="https://github.com/facumesa" target="_blank" class="px-6 py-3.5 rounded-xl bg-neutral-900/80 text-neutral-200 border border-neutral-800 hover:border-neutral-600 transition-all duration-200">
            GitHub ↗
          </a>
          <a href="https://www.linkedin.com/in/facundomesati/" target="_blank" class="px-6 py-3.5 rounded-xl bg-neutral-900/80 text-neutral-200 border border-neutral-800 hover:border-neutral-600 transition-all duration-200">
            LinkedIn ↗
          </a>
        </div>
      </header>

      <!-- CAPA SUPERIOR QUE SE SUPERPONE AL HACER SCROLL (z-10 y background sólido) -->
      <main id="contenido" class="relative z-10 bg-neutral-950 pt-24 pb-32 space-y-32 rounded-t-[3rem] border-t border-neutral-800/80 shadow-[0_-30px_60px_rgba(0,0,0,0.9)] -mx-6 sm:-mx-10 px-6 sm:px-10">
        
        <!-- SECCIÓN 1: PROYECTOS DESTACADOS -->
        <section class="space-y-12">
          <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 border-b border-neutral-800 pb-6">
            <div>
              <h2 class="text-3xl sm:text-4xl font-extrabold tracking-tight text-white">
                Proyectos Destacados
              </h2>
              <p class="text-base text-neutral-400 mt-2">
                Sistemas backend, soluciones enterprise y algoritmos de bajo nivel.
              </p>
            </div>
            <span class="text-xs font-mono text-neutral-500">01 / 03</span>
          </div>

          <div class="grid grid-cols-1 gap-10">
            {proyectosPrincipales.map(p => (
              <article class="group relative p-8 sm:p-10 rounded-3xl bg-neutral-900/60 border border-neutral-800/80 hover:border-neutral-600 transition-all duration-500 hover:shadow-2xl hover:-translate-y-1 backdrop-blur-md">
                <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3 mb-4">
                  <div>
                    <h3 class="text-2xl sm:text-3xl font-bold text-white group-hover:text-indigo-300 transition-colors">
                      {p.titulo}
                    </h3>
                    <p class="text-sm font-mono text-neutral-400 mt-1">{p.subtitulo}</p>
                  </div>
                  {p.destacado && (
                    <span class="w-fit px-3 py-1 rounded-full text-xs font-mono font-semibold uppercase tracking-wider bg-amber-500/10 text-amber-400 border border-amber-500/30">
                      ★ Featured Project
                    </span>
                  )}
                </div>
                
                <p class="text-base text-neutral-300 leading-relaxed mb-8 font-light">
                  {p.descripcion}
                </p>

                <div class="flex flex-wrap gap-2.5 mb-8">
                  {p.tags.map(tag => (
                    <span class={`px-3 py-1.5 text-xs font-mono font-medium rounded-lg border ${tag.color}`}>
                      {tag.nombre}
                    </span>
                  ))}
                </div>

                <div class="flex flex-wrap items-center gap-6 text-sm font-semibold pt-6 border-t border-neutral-800/80">
                  {p.linkGithub && (
                    <a href={p.linkGithub} target="_blank" class="text-neutral-300 hover:text-white transition-colors flex items-center gap-2 group/link">
                      <span>Ver en GitHub</span>
                      <span class="group-hover/link:translate-x-1 transition-transform">→</span>
                    </a>
                  )}
                  {p.linkDemo && (
                    <a href={p.linkDemo} target="_blank" class="text-emerald-400 hover:text-emerald-300 transition-colors flex items-center gap-2 group/link">
                      <span class="w-2 h-2 rounded-full bg-emerald-400 animate-ping"></span>
                      <span>Probar App Deployada</span>
                      <span class="group-hover/link:translate-x-1 transition-transform">→</span>
                    </a>
                  )}
                </div>
              </article>
            ))}
          </div>
        </section>

        <!-- SECCIÓN 2: PROYECTOS AI-ASSISTED -->
        <section class="space-y-12">
          <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 border-b border-neutral-800 pb-6">
            <div>
              <h2 class="text-3xl sm:text-4xl font-extrabold tracking-tight text-white">
                AI-Assisted & Rapid Prototyping
              </h2>
              <p class="text-base text-neutral-400 mt-2">
                Dirección de producto, Prompt Engineering técnico y aceleración con LLMs.
              </p>
            </div>
            <span class="text-xs font-mono text-neutral-500">02 / 03</span>
          </div>

          <div class="grid grid-cols-1 gap-8">
            {proyectosIA.map(p => (
              <article class="p-8 sm:p-10 rounded-3xl bg-neutral-900/40 border border-neutral-800/60 hover:border-neutral-700 transition-all duration-300">
                <h3 class="text-2xl font-bold text-white mb-2">{p.titulo}</h3>
                <p class="text-xs font-mono text-neutral-500 mb-4">{p.subtitulo}</p>
                <p class="text-base text-neutral-400 font-light leading-relaxed mb-6">{p.descripcion}</p>
                
                <div class="flex flex-wrap gap-2.5">
                  {p.tags.map(tag => (
                    <span class={`px-3 py-1.5 text-xs font-mono font-medium rounded-lg border ${tag.color}`}>
                      {tag.nombre}
                    </span>
                  ))}
                </div>
              </article>
            ))}
          </div>
        </section>

        <!-- SECCIÓN 3: TECH STACK Y HABILIDADES -->
        <section class="space-y-12">
          <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 border-b border-neutral-800 pb-6">
            <div>
              <h2 class="text-3xl sm:text-4xl font-extrabold tracking-tight text-white">
                Habilidades & Tecnologías
              </h2>
              <p class="text-base text-neutral-400 mt-2">
                Herramientas y lenguajes que utilizo en el día a día.
              </p>
            </div>
            <span class="text-xs font-mono text-neutral-500">03 / 03</span>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            {habilidades.map(group => (
              <div class="p-6 rounded-2xl bg-neutral-900/50 border border-neutral-800/60 space-y-4">
                <h3 class="text-sm font-mono text-neutral-400 uppercase tracking-wider">{group.categoria}</h3>
                <ul class="space-y-2">
                  {group.items.map(item => (
                    <li class="text-base text-neutral-200 font-medium flex items-center gap-2">
                      <span class="text-neutral-600">•</span> {item}
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </section>

        <!-- FOOTER -->
        <footer class="pt-16 border-t border-neutral-800/80 flex flex-col sm:flex-row items-center justify-between gap-6 text-sm text-neutral-500 font-mono">
          <p>© 2026 Facundo Mesa — Montevideo, Uruguay</p>
          <div class="flex items-center gap-6">
            <a href="https://github.com/facumesa" target="_blank" class="hover:text-white transition-colors">GitHub</a>
            <a href="https://www.linkedin.com/in/facundomesati/" target="_blank" class="hover:text-white transition-colors">LinkedIn</a>
          </div>
        </footer>

      </main>

    </div>
	<!-- CANVAS DE FONDO COMPLETO -->
<canvas id="equalizer-canvas" class="fixed inset-0 pointer-events-none -z-10 opacity-20"></canvas>

<script>
  const canvas = document.getElementById('equalizer-canvas');
  const ctx = canvas.getContext('2d');

  let width, height;
  let mouse = { x: -1000, y: -1000 };

  // --- CONFIGURACIÓN DE TAMAÑO GRANDE ---
  const BAR_WIDTH = 36;          // Ancho de los bloques (mucho más gruesos)
  const BAR_GAP = 18;            // Espacio entre bloques
  const MAX_HEIGHT_RATIO = 0.85; // Altura máxima (hasta el 85% de la pantalla)
  const MIN_HEIGHT = 40;         // Altura mínima fija de reposo
  const CORNER_RADIUS = 8;       // Bordes ligeramente redondeados estilo card

  let bars = [];

  function resize() {
    width = canvas.width = window.innerWidth;
    height = canvas.height = window.innerHeight;
    initBars();
  }

  function initBars() {
    bars = [];
    const barTotalWidth = BAR_WIDTH + BAR_GAP;
    const barCount = Math.ceil(width / barTotalWidth) + 1;

    for (let i = 0; i < barCount; i++) {
      bars.push({
        x: i * barTotalWidth,
        currentHeight: MIN_HEIGHT,
        targetHeight: MIN_HEIGHT,
        phase: Math.random() * Math.PI * 2,
        speed: 0.015 + Math.random() * 0.02
      });
    }
  }

  // Capturar posición del mouse
  window.addEventListener('mousemove', (e) => {
    mouse.x = e.clientX;
    mouse.y = e.clientY;
  });

  window.addEventListener('mouseleave', () => {
    mouse.x = -1000;
    mouse.y = -1000;
  });

  window.addEventListener('resize', resize);
  resize();

  function animate() {
    ctx.clearRect(0, 0, width, height);

    const centerY = height / 2;
    const maxBarHeight = height * MAX_HEIGHT_RATIO;

    bars.forEach((bar) => {
      // 1. Ritmo suave en reposo
      bar.phase += bar.speed;
      const idleWave = (Math.sin(bar.phase) + 1) / 2;
      let calculatedTarget = MIN_HEIGHT + idleWave * (maxBarHeight * 0.15);

      // 2. Reacción al paso del cursor (área de efecto expandida)
      const barCenterX = bar.x + BAR_WIDTH / 2;
      const distPercentX = Math.abs(mouse.x - barCenterX);
      const distPercentY = Math.abs(mouse.y - centerY);

      const radiusX = 350; // Mayor rango de respuesta horizontal
      const radiusY = 450; // Mayor rango de respuesta vertical

      if (distPercentX < radiusX && distPercentY < radiusY) {
        const factorX = 1 - (distPercentX / radiusX);
        const factorY = 1 - (distPercentY / radiusY);
        const boost = factorX * factorY * (maxBarHeight - MIN_HEIGHT);
        calculatedTarget += boost;
      }

      // Transición fluida
      bar.targetHeight = calculatedTarget;
      bar.currentHeight += (bar.targetHeight - bar.currentHeight) * 0.08;

      // 3. Renderizado del rectángulo
      const barY = centerY - (bar.currentHeight / 2);

      // Gradiente sobrio estilo Tailwind
      const gradient = ctx.createLinearGradient(0, barY, 0, barY + bar.currentHeight);
      gradient.addColorStop(0, 'rgba(99, 102, 241, 0.6)');  // Indigo-500
      gradient.addColorStop(0.5, 'rgba(168, 85, 247, 0.75)'); // Purple-500
      gradient.addColorStop(1, 'rgba(99, 102, 241, 0.6)');

      ctx.fillStyle = gradient;

      // Rectángulo con esquinas redondeadas
      ctx.beginPath();
      if (ctx.roundRect) {
        ctx.roundRect(bar.x, barY, BAR_WIDTH, bar.currentHeight, CORNER_RADIUS);
      } else {
        ctx.rect(bar.x, barY, BAR_WIDTH, bar.currentHeight);
      }
      ctx.fill();
    });

    requestAnimationFrame(animate);
  }

  animate();
</script>
  </body>
</html>