---
import ImageCarousel from '../components/ImageCarousel.astro';
import ImageLightbox from '../components/ImageLightbox.astro';
import Hero from '../components/Hero.astro';
import Profile from '../components/Profile.astro';
import ExperienceEducation from '../components/ExperienceEducation.astro';
import { habilidades, proyectosIA, proyectosPrincipales } from '../data/portfolio';
import '../styles/global.css';
---

<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Facundo Mesa | Software Developer</title>
  </head>
  <body class="min-h-screen text-neutral-100 font-sans overflow-x-hidden">
    <div class="max-w-7xl mx-auto px-4 sm:px-8">
      
      <Hero />

      <!-- CAPA SUPERIOR QUE SE SUPERPONE AL HACER SCROLL (z-10 y background sólido) -->
      <main id="contenido" class="relative pb-20">

        <Profile />

        <ExperienceEducation />
        
        <!-- SECCIÓN 1: PROYECTOS DESTACADOS -->
        <section id="proyectos-destacados" class="space-y-12 scroll-mt-10 border-x border-b border-white/10 px-5 py-16 sm:p-10 lg:p-16">
          <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 border-b border-white/10 pb-6">
            <div>
              <p class="section-kicker mb-4">03 / Trabajo</p>
              <h2 class="text-4xl sm:text-6xl font-black uppercase tracking-[-0.05em] text-white">
                Proyectos Destacados
              </h2>
              <p class="text-base text-neutral-400 mt-2">
                Sistemas backend, soluciones enterprise y algoritmos de bajo nivel.
              </p>
            </div>
            <span class="text-xs font-mono text-neutral-500">01 / 03</span>
          </div>

          <div class="grid grid-cols-1 gap-10">
            {proyectosPrincipales.map(p => (
              <article class="group relative p-8 sm:p-10 bg-[#0c0f0d] border border-white/10 hover:border-lime-300/60 transition-all duration-500 hover:-translate-y-1 hover:shadow-[8px_8px_0_0_rgba(217,255,87,0.12)]">
                <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3 mb-4">
                  <div>
                    <h3 class="text-2xl sm:text-3xl font-bold text-white group-hover:text-lime-300 transition-colors">
                      {p.titulo}
                    </h3>
                    <p class="text-sm font-mono text-neutral-400 mt-1">{p.subtitulo}</p>
                  </div>
                  {p.destacado && (
                    <span class="w-fit px-3 py-1 text-xs font-mono font-semibold uppercase tracking-wider text-lime-300 border border-lime-300/50">
                      Proyecto principal
                    </span>
                  )}
                </div>
                
                <p class="text-base text-neutral-300 leading-relaxed mb-8 font-light">
                  {p.descripcion}
                </p>

                {p.notaDiseno && (
                  <div class="mb-8 p-5 border-l-2 border-lime-300 bg-lime-300/[0.03]">
                    <p class="text-xs font-mono uppercase tracking-widest text-lime-300 mb-2">Nota sobre la interfaz</p>
                    <p class="text-sm text-neutral-400 leading-relaxed">{p.notaDiseno}</p>
                  </div>
                )}

                {p.capturas && (
                  <div class="mb-8"><ImageCarousel images={p.capturas} label="Capturas del sistema" /></div>
                )}

                {p.flujo && (
                  <div class="mb-8 p-6 border border-white/10 bg-black/20">
                    <p class="text-xs font-mono uppercase tracking-widest text-neutral-500 mb-5">Flujo principal del sistema</p>
                    <ol class="grid sm:grid-cols-4 gap-3">
                      {p.flujo.map((paso, index) => (
                        <li class="relative p-4 border-t border-white/10 bg-transparent text-sm text-neutral-300">
                          <span class="block text-xs font-mono text-lime-300 mb-2">0{index + 1}</span>
                          {paso}
                        </li>
                      ))}
                    </ol>
                    <p class="text-sm text-neutral-500 leading-relaxed mt-5">Las listas enlazadas mantienen los vuelos ordenados; pilas y colas modelan operaciones y estados sin recurrir a las colecciones nativas de Java.</p>
                  </div>
                )}

                <div class="flex flex-wrap gap-2.5 mb-8">
                  {p.tags.map(tag => (
                    <span class="px-3 py-1.5 text-xs font-mono font-medium uppercase tracking-wider border border-white/15 text-neutral-300">
                      {tag.nombre}
                    </span>
                  ))}
                </div>

                <div class="flex flex-wrap items-center gap-6 text-sm font-semibold pt-6 border-t border-white/10">
                  {p.linkGithub && (
                    <a href={p.linkGithub} target="_blank" class="text-neutral-300 hover:text-white transition-colors flex items-center gap-2 group/link">
                      <span>Ver en GitHub</span>
                      <span class="group-hover/link:translate-x-1 transition-transform">→</span>
                    </a>
                  )}
                  {p.linkDemo && (
                    <a href={p.linkDemo} target="_blank" class="text-emerald-400 hover:text-emerald-300 transition-colors flex items-center gap-2 group/link">
                      <span class="w-2 h-2 rounded-full bg-emerald-400 animate-ping"></span>
                      <span>Probar App Deployada</span>
                      <span class="group-hover/link:translate-x-1 transition-transform">→</span>
                    </a>
                  )}
                </div>
              </article>
            ))}
          </div>
        </section>

        <!-- SECCIÓN 2: PROYECTOS PERSONALES ASISTIDOS POR IA -->
        <section class="space-y-12 border-x border-b border-white/10 px-5 py-16 sm:p-10 lg:p-16">
          <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 border-b border-white/10 pb-6">
            <div>
              <p class="section-kicker mb-4">04 / Laboratorio IA</p>
              <h2 class="text-4xl sm:text-6xl font-black uppercase tracking-[-0.05em] text-white">
                Proyectos Personales con IA
              </h2>
              <p class="text-base text-neutral-400 mt-2">
                Experimentos creados mediante prompts y asistencia intensiva de IA, separados de mi experiencia técnica principal.
              </p>
            </div>
            <span class="text-xs font-mono text-neutral-500">02 / 03</span>
          </div>

          <div class="grid grid-cols-1 gap-8">
            {proyectosIA.map(p => (
              <article class="group relative p-8 sm:p-10 bg-[#0c0f0d] border border-white/10 hover:border-lime-300/60 transition-all duration-500 hover:-translate-y-1 hover:shadow-[8px_8px_0_0_rgba(217,255,87,0.12)]">
                <h3 class="text-2xl font-bold text-white mb-2 group-hover:text-lime-300 transition-colors">{p.titulo}</h3>
                <p class="text-xs font-mono text-neutral-500 mb-4">{p.subtitulo}</p>
                <p class="text-base text-neutral-400 font-light leading-relaxed mb-6">{p.descripcion}</p>
                
                <div class="flex flex-wrap gap-2.5">
                  {p.tags.map(tag => (
                    <span class="px-3 py-1.5 text-xs font-mono font-medium uppercase tracking-wider border border-white/15 text-neutral-300">
                      {tag.nombre}
                    </span>
                  ))}
                </div>

                <div class="mt-8 pt-6 border-t border-white/10">
                  <a href={p.linkShowcase} class="text-sm font-semibold text-neutral-300 hover:text-white transition-colors inline-flex items-center gap-2 group/link">
                    <span>Ver caso de estudio</span>
                    <span class="group-hover/link:translate-x-1 transition-transform">→</span>
                  </a>
                </div>
              </article>
            ))}
          </div>
        </section>

        <!-- SECCIÓN 3: TECH STACK Y HABILIDADES -->
        <section class="space-y-12 border-x border-b border-white/10 px-5 py-16 sm:p-10 lg:p-16">
          <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 border-b border-white/10 pb-6">
            <div>
              <p class="section-kicker mb-4">05 / Stack</p>
              <h2 class="text-4xl sm:text-6xl font-black uppercase tracking-[-0.05em] text-white">
                Habilidades & Tecnologías
              </h2>
              <p class="text-base text-neutral-400 mt-2">
                Herramientas y lenguajes que utilizo en el día a día.
              </p>
            </div>
            <span class="text-xs font-mono text-neutral-500">03 / 03</span>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            {habilidades.map(group => (
              <div class="p-6 border-t border-white/15 space-y-5">
                <h3 class="text-xs font-mono text-lime-300 uppercase tracking-[0.16em]">{group.categoria}</h3>
                <ul class="space-y-2">
                  {group.items.map(item => (
                    <li class="text-base text-neutral-200 font-medium flex items-center gap-2">
                      <span class="text-lime-300">+</span> {item}
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </section>

        <!-- FOOTER -->
        <footer class="pt-16 border-t border-white/10 flex flex-col sm:flex-row items-center justify-between gap-6 text-sm text-neutral-500 font-mono">
          <p>© 2026 Facundo Mesa — Montevideo, Uruguay</p>
          <div class="flex items-center gap-6">
            <a href="https://github.com/facumesa" target="_blank" class="hover:text-white transition-colors">GitHub</a>
            <a href="https://www.linkedin.com/in/facundomesati/" target="_blank" class="hover:text-white transition-colors">LinkedIn</a>
          </div>
        </footer>

      </main>

    </div>
<ImageLightbox />
  </body>
</html>
